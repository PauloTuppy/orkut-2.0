name: Deploy to Staging

on:
  push:
    branches: [ develop ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Deploy to staging
        run: |
          echo "🚀 Deploy para staging"
          echo "Backend: https://orkut-backend-staging.herokuapp.com"
          echo "Frontend: https://orkut-frontend-staging.herokuapp.com"
          # Depois você vai integrar com Heroku/Railway/Render
